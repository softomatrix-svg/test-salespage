# SendMail Agent Skill

## What it does
Sends an email to ie@agentmail.to using SMTP.

## Setup
1. Install dependencies:
   npm install nodemailer

2. Update config.json with your email credentials

3. Use an App Password if using Gmail

## Example Input
{
  "subject": "Test Email",
  "message": "Hello from OpenClaw"
}
